---
title: "Introduction"
description: "Welcome to the YouTube RSS API"
---

## Welcome to the YouTube RSS API

The YouTube RSS API provides programmatic access to convert YouTube videos into audio. Our REST API enables you to integrate YouTube-to-audio conversion into your applications, automate feed management, and build custom podcast solutions.

## Base URL

All API requests should be made to:

```
https://ytrss.xyz/api/v1
```

## Authentication

The YouTube RSS API uses API key authentication. Include your API key in the `Authorization` header:

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://ytrss.xyz/api/v1/poll/job/1234
```

<Card title="Get Your API Key" icon="key" href="/guides/authentication">
    Learn how to obtain and use your API key
</Card>

## Quick Start

### 1. Convert a YouTube Video


```bash
curl -X POST https://ytrss.xyz/v1/convert \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "urls": ["https://youtube.com/watch?v=VIDEO_ID"],
  }'
```

### 2. Check Job Status

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://ytrss.xyz/v1/poll/job/{jobId}
```

### 3. Download Audio

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://ytrss.xyz/v1/download/{jobId}
```
